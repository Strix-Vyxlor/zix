# zix, a nix helper writen in zig

configure zix with a json file.
config at ```~/.config/zix/zix.conf```:
```
{
  "flake": false,
  "path": ".nix-config",
  "hostname": "none",
  "nix-on-droid": false,
  "root-command": "sudo",
}
```
